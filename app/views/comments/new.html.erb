<%= form_for [@picture, @comment] do |f| %>
  <%= f.label :thoughts %>
  <%= f.text_area :thoughts %>
  <%= f.label :like %>
  <%= f.select :like, (1..5) %>
  <%= f.submit 'Leave Comment' %>
<% end %>
